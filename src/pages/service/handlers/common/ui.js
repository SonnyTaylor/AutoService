/**
 * Common UI rendering utilities for service handlers.
 * Re-exported from the main renderers/common.js for handler convenience.
 */

export {
  renderHeader,
  renderList,
  prettifyKey,
  formatValue,
  kpiBox,
  pill,
} from "../../results/renderers/common.js";
